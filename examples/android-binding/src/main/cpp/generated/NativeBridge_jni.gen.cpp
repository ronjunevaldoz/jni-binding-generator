// AUTO-GENERATED by jni-binding-generator.py — DO NOT EDIT.
// Source: NativeBridge.kt
//
// Regenerate with:  ./gradlew generateJniBindings   (or run the script directly)
// Hand-written logic belongs in the corresponding *-jni.cpp, not here.

#include <jni.h>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>
#include <cstdint>

#include "jni-utils.h"


extern "C" JNIEXPORT jlong JNICALL
Java_com_example_myapp_NativeBridge_nativeCreate(
        JNIEnv* env,
        jclass clazz,
        jstring modelPath,
        jint threads,
        jboolean useGpu) {
    // --- Marshalling ---
    std::string modelPath_val = jstring2string(env, modelPath);
    if (env->ExceptionCheck()) return 0;
    int32_t threads_val = static_cast<int32_t>(threads);
    bool useGpu_val = (useGpu == JNI_TRUE);

    // --- Error handling ---
    if (modelPath_val.empty()) {
        throw_illegal_argument(env, "nativeCreate: modelPath is required");
        return 0;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return 0;
}


extern "C" JNIEXPORT void JNICALL
Java_com_example_myapp_NativeBridge_nativeDestroy(
        JNIEnv* env,
        jclass clazz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeDestroy: handle not initialized");
        return;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return;
}


extern "C" JNIEXPORT jstring JNICALL
Java_com_example_myapp_NativeBridge_nativeGenerate(
        JNIEnv* env,
        jclass clazz,
        jlong handle,
        jstring prompt,
        jint maxTokens) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::string prompt_val = jstring2string(env, prompt);
    if (env->ExceptionCheck()) return nullptr;
    int32_t maxTokens_val = static_cast<int32_t>(maxTokens);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGenerate: handle not initialized");
        return nullptr;
    }
    if (prompt_val.empty()) {
        throw_illegal_argument(env, "nativeGenerate: prompt is required");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT jintArray JNICALL
Java_com_example_myapp_NativeBridge_nativeGenerateTokens(
        JNIEnv* env,
        jclass clazz,
        jlong handle,
        jstring prompt,
        jint maxTokens) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::string prompt_val = jstring2string(env, prompt);
    if (env->ExceptionCheck()) return nullptr;
    int32_t maxTokens_val = static_cast<int32_t>(maxTokens);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGenerateTokens: handle not initialized");
        return nullptr;
    }
    if (prompt_val.empty()) {
        throw_illegal_argument(env, "nativeGenerateTokens: prompt is required");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT jstring JNICALL
Java_com_example_myapp_NativeBridge_nativeGetModelName(
        JNIEnv* env,
        jclass clazz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetModelName: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT jobject JNICALL
Java_com_example_myapp_NativeBridge_nativeGetMetadata(
        JNIEnv* env,
        jclass clazz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetMetadata: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    // Return: use make_map_string_string(env, yourResult) to build the jobject.
    return nullptr;
}


extern "C" JNIEXPORT jint JNICALL
Java_com_example_myapp_NativeBridge_nativeGetVocabSize(
        JNIEnv* env,
        jclass clazz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetVocabSize: handle not initialized");
        return 0;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return 0;
}


extern "C" JNIEXPORT jint JNICALL
Java_com_example_myapp_NativeBridge_nativeGetContextSize(
        JNIEnv* env,
        jclass clazz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetContextSize: handle not initialized");
        return 0;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return 0;
}


extern "C" JNIEXPORT jobject JNICALL
Java_com_example_myapp_NativeBridge_nativeEmbed(
        JNIEnv* env,
        jclass clazz,
        jlong handle,
        jstring text) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::string text_val = jstring2string(env, text);
    if (env->ExceptionCheck()) return nullptr;

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeEmbed: handle not initialized");
        return nullptr;
    }
    if (text_val.empty()) {
        throw_illegal_argument(env, "nativeEmbed: text is required");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    // Return: use make_list_float(env, yourResult) to build the jobject.
    return nullptr;
}


extern "C" JNIEXPORT jobject JNICALL
Java_com_example_myapp_NativeBridge_nativeBatchEmbed(
        JNIEnv* env,
        jclass clazz,
        jlong handle,
        jobjectArray texts) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::vector<std::string> texts_val = extract_string_array(env, texts);
    if (env->ExceptionCheck()) return nullptr;

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeBatchEmbed: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    // Return: use make_list_list_float(env, yourResult) to build the jobject.
    return nullptr;
}


extern "C" JNIEXPORT void JNICALL
Java_com_example_myapp_NativeBridge_nativeSetSamplerParams(
        JNIEnv* env,
        jclass clazz,
        jlong handle,
        jfloat temperature,
        jfloat topP,
        jint topK) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    float temperature_val = static_cast<float>(temperature);
    float topP_val = static_cast<float>(topP);
    int32_t topK_val = static_cast<int32_t>(topK);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeSetSamplerParams: handle not initialized");
        return;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return;
}


extern "C" JNIEXPORT jobject JNICALL
Java_com_example_myapp_NativeBridge_nativeGetSamplerParams(
        JNIEnv* env,
        jclass clazz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetSamplerParams: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    // Return: use make_map_string_float(env, yourResult) to build the jobject.
    return nullptr;
}


extern "C" JNIEXPORT jintArray JNICALL
Java_com_example_myapp_NativeBridge_nativeTokenize(
        JNIEnv* env,
        jclass clazz,
        jlong handle,
        jstring text) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::string text_val = jstring2string(env, text);
    if (env->ExceptionCheck()) return nullptr;

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeTokenize: handle not initialized");
        return nullptr;
    }
    if (text_val.empty()) {
        throw_illegal_argument(env, "nativeTokenize: text is required");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT jstring JNICALL
Java_com_example_myapp_NativeBridge_nativeDetokenize(
        JNIEnv* env,
        jclass clazz,
        jlong handle,
        jintArray tokens) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::vector<int32_t> tokens_val = extract_int_array(env, tokens);
    if (env->ExceptionCheck()) return nullptr;

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeDetokenize: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}
