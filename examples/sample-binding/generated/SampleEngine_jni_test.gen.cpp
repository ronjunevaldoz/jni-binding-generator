// AUTO-GENERATED by jni-binding-generator.py — DO NOT EDIT.
// Source: SampleEngine.kt
//
// Compile-time type check for jni-utils.h helpers used by SampleEngine.kt.
// Every helper call lives inside ``if (false)`` so the compiler type-checks
// the call without executing it — any signature mismatch is a compile error.
//
// Compile (syntax check only):
//   clang++ -std=c++17 -fsyntax-only \
//       -I$JAVA_HOME/include -I$JAVA_HOME/include/$(uname -s | tr '[:upper:]' '[:lower:]') \
//       -I<dir-containing-jni-utils.h> SampleEngine_jni_test.gen.cpp

#include <jni.h>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>

#include "jni-utils.h"

#if defined(__GNUC__) || defined(__clang__)
#  pragma GCC diagnostic ignored "-Wunused-variable"
#endif

static void _compile_check_SampleEngine(JNIEnv* env) {
    // nativeLoad
    if (false) {
        jstring modelPath = nullptr;
        std::string modelPath_val = jstring2string(env, modelPath);
        jint threads = 0;
        int32_t threads_val = static_cast<int32_t>(threads);
    }
    // nativeProcess
    if (false) {
        jlong handle = 0;
        void* handle_ptr = reinterpret_cast<void*>(handle);
        jstring input = nullptr;
        std::string input_val = jstring2string(env, input);
        jint timeout = 0;
        int32_t timeout_val = static_cast<int32_t>(timeout);
        jfloat temperature = 0;
        float temperature_val = static_cast<float>(temperature);
    }
    // nativeTokenizeBatch
    if (false) {
        jlong handle = 0;
        void* handle_ptr = reinterpret_cast<void*>(handle);
        jobjectArray prompts = nullptr;
        std::vector<std::string> prompts_val = extract_string_array(env, prompts);
        jboolean addBos = 0;
        bool addBos_val = (addBos == JNI_TRUE);
    }
    // nativeRelease
    if (false) {
        jlong handle = 0;
        void* handle_ptr = reinterpret_cast<void*>(handle);
    }
    // nativeGetTags
    if (false) {
        jlong handle = 0;
        void* handle_ptr = reinterpret_cast<void*>(handle);
        std::vector<std::string> _ret_val{};
        jobject _ret = make_list_string(env, _ret_val);
    }
    // nativeSearch
    if (false) {
        jlong handle = 0;
        void* handle_ptr = reinterpret_cast<void*>(handle);
        jstring query = nullptr;
        std::string query_val = jstring2string(env, query);
        jobject options = nullptr;
        std::unordered_map<std::string, std::string> options_val = extract_map_string_string(env, options);
        std::vector<std::string> _ret_val{};
        jobject _ret = make_list_string(env, _ret_val);
    }
    // nativeSetStrategy
    if (false) {
        jlong handle = 0;
        void* handle_ptr = reinterpret_cast<void*>(handle);
        jobject strategy = nullptr;
        int32_t strategy_val = enum_ordinal(env, strategy);
    }
    // nativeGetStrategy
    if (false) {
        jlong handle = 0;
        void* handle_ptr = reinterpret_cast<void*>(handle);
    }
}

int main() { return 0; }
