// AUTO-GENERATED by jni-binding-generator.py — DO NOT EDIT.
// Source: SampleEngine.kt
//
// Regenerate with:  ./gradlew generateJniBindings   (or run the script directly)
// Hand-written logic belongs in the corresponding *-jni.cpp, not here.

#include <jni.h>
#include <string>
#include <vector>
#include <cstdint>

#include "jni-utils.h"


extern "C" JNIEXPORT jlong JNICALL
Java_com_example_sample_SampleEngine_nativeLoad(
        JNIEnv* env,
        jobject thiz,
        jstring modelPath,
        jint threads) {
    // --- Marshalling ---
    std::string modelPath_val = jstring2string(env, modelPath);
    int32_t threads_val = static_cast<int32_t>(threads);

    // --- Error handling ---
    if (modelPath_val.empty()) {
        throw_illegal_argument(env, "nativeLoad: modelPath is required");
        return 0;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return 0;
}


extern "C" JNIEXPORT jbyteArray JNICALL
Java_com_example_sample_SampleEngine_nativeProcess(
        JNIEnv* env,
        jobject thiz,
        jlong handle,
        jstring input,
        jint timeout,
        jfloat temperature) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::string input_val = jstring2string(env, input);
    int32_t timeout_val = static_cast<int32_t>(timeout);
    float temperature_val = static_cast<float>(temperature);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeProcess: handle not initialized");
        return nullptr;
    }
    if (input_val.empty()) {
        throw_illegal_argument(env, "nativeProcess: input is required");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT jintArray JNICALL
Java_com_example_sample_SampleEngine_nativeTokenizeBatch(
        JNIEnv* env,
        jobject thiz,
        jlong handle,
        jobjectArray prompts,
        jboolean addBos) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::vector<std::string> prompts_val = extract_string_array(env, prompts);
    bool addBos_val = (addBos == JNI_TRUE);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeTokenizeBatch: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT void JNICALL
Java_com_example_sample_SampleEngine_nativeRelease(
        JNIEnv* env,
        jobject thiz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeRelease: handle not initialized");
        return;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return;
}
