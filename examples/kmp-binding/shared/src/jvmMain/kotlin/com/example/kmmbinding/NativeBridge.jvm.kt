package com.example.kmmbinding

// jvmMain is shared by any JVM-targeted source set that isn't
// desktopMain-specific (e.g. a future :serverApp module).
//
// The desktop actual is in desktopMain/NativeBridge.desktop.kt.
// If you add another jvm() target, provide its actual here instead.
