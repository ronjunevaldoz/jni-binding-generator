// AUTO-GENERATED by jni-binding-generator.py — DO NOT EDIT.
// Source: NativeBridgeJni.kt
//
// Regenerate with:  ./gradlew generateJniBindings   (or run the script directly)
// Hand-written logic belongs in the corresponding *-jni.cpp, not here.

#include <jni.h>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>
#include <cstdint>

#include "jni-utils.h"


extern "C" JNIEXPORT jlong JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeCreate(
        JNIEnv* env,
        jobject thiz,
        jstring modelPath,
        jint threads,
        jboolean useGpu) {
    // --- Marshalling ---
    std::string modelPath_val = jstring2string(env, modelPath);
    if (env->ExceptionCheck()) return 0;
    int32_t threads_val = static_cast<int32_t>(threads);
    bool useGpu_val = (useGpu == JNI_TRUE);

    // --- Error handling ---
    if (modelPath_val.empty()) {
        throw_illegal_argument(env, "nativeCreate: modelPath is required");
        return 0;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return 0;
}


extern "C" JNIEXPORT void JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeDestroy(
        JNIEnv* env,
        jobject thiz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeDestroy: handle not initialized");
        return;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return;
}


extern "C" JNIEXPORT jstring JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeGenerate(
        JNIEnv* env,
        jobject thiz,
        jlong handle,
        jstring prompt,
        jint maxTokens) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::string prompt_val = jstring2string(env, prompt);
    if (env->ExceptionCheck()) return nullptr;
    int32_t maxTokens_val = static_cast<int32_t>(maxTokens);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGenerate: handle not initialized");
        return nullptr;
    }
    if (prompt_val.empty()) {
        throw_illegal_argument(env, "nativeGenerate: prompt is required");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT jintArray JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeTokenize(
        JNIEnv* env,
        jobject thiz,
        jlong handle,
        jstring text) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::string text_val = jstring2string(env, text);
    if (env->ExceptionCheck()) return nullptr;

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeTokenize: handle not initialized");
        return nullptr;
    }
    if (text_val.empty()) {
        throw_illegal_argument(env, "nativeTokenize: text is required");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT jstring JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeDetokenize(
        JNIEnv* env,
        jobject thiz,
        jlong handle,
        jintArray tokens) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::vector<int32_t> tokens_val = extract_int_array(env, tokens);
    if (env->ExceptionCheck()) return nullptr;

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeDetokenize: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return nullptr;
}


extern "C" JNIEXPORT jobject JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeEmbed(
        JNIEnv* env,
        jobject thiz,
        jlong handle,
        jstring text) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    std::string text_val = jstring2string(env, text);
    if (env->ExceptionCheck()) return nullptr;

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeEmbed: handle not initialized");
        return nullptr;
    }
    if (text_val.empty()) {
        throw_illegal_argument(env, "nativeEmbed: text is required");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    // Return: use make_list_float(env, yourResult) to build the jobject.
    return nullptr;
}


extern "C" JNIEXPORT jobject JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeGetMetadata(
        JNIEnv* env,
        jobject thiz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetMetadata: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    // Return: use make_map_string_string(env, yourResult) to build the jobject.
    return nullptr;
}


extern "C" JNIEXPORT jobject JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeGetSamplerParams(
        JNIEnv* env,
        jobject thiz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetSamplerParams: handle not initialized");
        return nullptr;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    // Return: use make_map_string_float(env, yourResult) to build the jobject.
    return nullptr;
}


extern "C" JNIEXPORT void JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeSetSamplerParams(
        JNIEnv* env,
        jobject thiz,
        jlong handle,
        jfloat temperature,
        jfloat topP,
        jint topK) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);
    float temperature_val = static_cast<float>(temperature);
    float topP_val = static_cast<float>(topP);
    int32_t topK_val = static_cast<int32_t>(topK);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeSetSamplerParams: handle not initialized");
        return;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return;
}


extern "C" JNIEXPORT jint JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeGetVocabSize(
        JNIEnv* env,
        jobject thiz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetVocabSize: handle not initialized");
        return 0;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return 0;
}


extern "C" JNIEXPORT jint JNICALL
Java_com_example_kmmbinding_NativeBridgeJni_nativeGetContextSize(
        JNIEnv* env,
        jobject thiz,
        jlong handle) {
    // --- Marshalling ---
    void* handle_ptr = reinterpret_cast<void*>(handle);

    // --- Error handling ---
    if (!handle_ptr) {
        throw_illegal_state(env, "nativeGetContextSize: handle not initialized");
        return 0;
    }

    // --- TODO: hand-written native logic ---
    // Call into your native library using the marshalled values above.
    return 0;
}
